name: Spellcheck
on:
  workflow_call:

jobs:
  spelling:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Spellcheck
      uses: SFLScientific/spellcheck-github-actions@master
